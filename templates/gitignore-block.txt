# >>> harness-eigenbau (vom Onboarding gesetzt) >>>
# Der Harness-Ordner ist Fremd-Klon + rechnerlokale Konfiguration und bleibt
# draussen. Die EIGENBAUTEN werden getrackt — sonst liegen sie ohne Historie
# nur auf der Platte, und der erste Rechnerwechsel loescht sie.
#
# ACHTUNG: sichtbar ist nicht gesichert. Diese Zeilen erlauben git nur, die
# Dateien zu SEHEN. Aufgenommen werden sie durch `git add`, gesichert erst
# durch Commit und Push.
#
# Neuer Eigenbau => hier eintragen, sonst ist er wieder ungesichert.
.claude/*
!.claude/*.js
!.claude/settings.json
!.claude/commands/
.claude/commands/*
!.claude/commands/repo-status.md
!.claude/commands/save-work.md
!.claude/commands/session-map.md
!.claude/commands/tell-session.md
!.claude/commands/onboarding.md
!.claude/skills/
.claude/skills/*
!.claude/skills/domain-modeling/
!.claude/skills/resolving-merge-conflicts/
!.claude/skills/i-have-adhd/
!.claude/rules/
.claude/rules/*
!.claude/rules/keel/
.claude/rules/keel/*
!.claude/rules/keel/no-oneshot.md
!.claude/rules/keel/completeness.md
!.claude/rules/keel/tools.md
!.claude/rules/keel/output-shape.md
!.claude/rules/keel/working-method.md

# Rechnerlokal, NIE committen: die Befehls-Freigabeliste.
.claude/settings.local.json

# Der ECC-Fremd-Klon (Update-Quelle), falls er spaeter danebengelegt wird.
.ecc-src/

# Die Dashboard ist ein STAND, kein Quelltext. Eine eingecheckte Fassung
# waere sofort veraltet und wuerde trotzdem als aktuell gelesen.
dashboard.html
dashboard.json

# Zugaenge: nie.
.env
.env.*
!.env.example
*.key
*.pem
secrets.json
credentials.json
*service-account*.json

node_modules/
.DS_Store
# <<< harness-eigenbau (vom Onboarding gesetzt) <<<
